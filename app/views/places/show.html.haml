.row
  .column.small-12
    %h1= @place.name
.row
  .small-12.columns
    %h4
      Characters from
      = @place.name
    %ul
      - if @place.birthplace_of.each do |c|
        %li= link_to c.name, c
      - end.empty?
        %li none

